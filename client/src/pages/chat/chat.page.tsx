const Chat = () => {
  return (
    <div>
      <div>
        {/* Chat info (other user's name, online status and a back button that's basically a Link to /) */}
      </div>
      <div>{/* Message bubbles container */}</div>
      <div>{/* Form (input and submit) */}</div>
    </div>
  );
};

export default Chat;
